import { RowStats } from './row-stats.model';


describe('RowStats', () => {
    let rowStats = new RowStats(undefined, undefined, undefined, undefined, undefined, undefined, undefined);

    beforeEach(() => {
    });


});

